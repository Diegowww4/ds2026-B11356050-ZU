services:
  backend:
    image: node:20-alpine
    container_name: kinesio-tape-line-helper-backend
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    command: sh -c "npm install && npm run dev"
